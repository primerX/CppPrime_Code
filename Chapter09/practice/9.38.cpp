#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char const *argv[])
{
	vector<int> vi;
	int s, c;

	for(s = vi.size(), c = vi.capacity(); s <= c; s++){
		v.push_back(1);
	}
	cout << "空间: " << vi.capacity() << "  元素数量: " << vi.size() << endl;
	return 0;
}